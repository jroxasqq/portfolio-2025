import Header from "@/components/Header";

const page = () => {
  return (
    <div className="h-screen bg-gray-100 items-center flex flex-col">
      <Header />
      <div>projects</div>
    </div>
  );
};

export default page;
